BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes*/*/*.bb \
            ${LAYERDIR}/recipes*/*/*.bbappend"

BBFILE_COLLECTIONS += "xmz-mod-touch"
BBFILE_PATTERN_xmz-mod-touch := "^${LAYERDIR}/"
BBFILE_PRIORITY_xmz-mod-touch = "6"

